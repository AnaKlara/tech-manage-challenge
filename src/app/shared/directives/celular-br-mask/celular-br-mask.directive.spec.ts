import { CelularBrMaskDirective } from './celular-br-mask.directive';

describe('CelularBrMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CelularBrMaskDirective();
    expect(directive).toBeTruthy();
  });
});
