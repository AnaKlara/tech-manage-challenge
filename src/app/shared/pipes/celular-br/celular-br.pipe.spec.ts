import { CelularBrPipe } from './celular-br.pipe';

describe('CelularBrPipe', () => {
  it('create an instance', () => {
    const pipe = new CelularBrPipe();
    expect(pipe).toBeTruthy();
  });
});
